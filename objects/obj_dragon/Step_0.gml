/*var i= random(10);
if ((i == 0) && (cd <1)){
	instance_create_layer(x,y, "SpearLayer", obj_fireball);
	cd = 0;
}else {
	if (instance_ exist (obj_player()){
		move_towards_point(obj_player.x, obj_player.y, sp);
		image_angle = direction;
	}
	cd = cd -1;
	
	if (hpoint <=0){
		instance_destroy();
	}
	*/